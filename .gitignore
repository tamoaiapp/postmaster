# Build output
dist/
out/
dist-build.log

# Dependencies
node_modules/

# Playwright junctions (CI cria via npx playwright install + mklink)
ms-playwright/

# Electron temp
.electron/

# Recovery / debug artifacts
_def_test/
_pm_*/
*.tmp
*.log

# Local dev
.env
.env.local

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Outputs do PostMaster userData (não devem estar no repo)
postmaster-data/

# Binarios externos baixados no build (todos via CI release.yml).
# Inclui deno.exe (122MB), yt-dlp.exe, whisper-cli + DLLs + ggml-*.bin (~300MB),
# piper.exe + vozes pt_BR-faber/cadu-medium (~190MB).
bin/*
!bin/.gitkeep

# Scripts locais de teste (nao publicar)
test-*.mjs

# Scripts privados não-versionados
scripts/_*
